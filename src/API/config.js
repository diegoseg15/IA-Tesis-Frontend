// Ruta base de la API, indicando la dirección del servidor y el punto de acceso a la API
export const basePath = "http://localhost:3977/api";

// Versión de la API que se utilizará en la construcción de las URLs de las solicitudes
export const apiVersion = "v1";
